# Tomcat
server:
    tomcat:
        max-threads: 10
        min-spare-threads: 10
    port: 8091

# mysql
spring:
    datasource:
        type: com.alibaba.druid.pool.DruidDataSource
        driverClassName: com.mysql.jdbc.Driver
        url: jdbc:mysql://116.62.213.152:3306/xboost?useUnicode=true&characterEncoding=UTF-8
        username: yumni
        password: Wyz123!@#
    jackson:
        time-zone: GMT+8
        date-format: yyyy-MM-dd HH:mm:ss
    resources:
            static-locations: classpath:/static/,classpath:/views/

# Mybatis配置
mybatis:
    mapperLocations: classpath:mapper/**/*.xml

